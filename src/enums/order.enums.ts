export enum OrderStatus {
    PENDING = "pending",
    SHIPPED = "shipped",
    DELIVERED = "delivered",
    CANCELLED = "cancelled"
}

export enum OrderTypes {
    PRODUCT_ORDER = 'ProductOrder',
    MEAL_ORDER = 'MealOrder'
}
